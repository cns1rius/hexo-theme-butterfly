/*
 * edu-analysis-crc32rev.js — CRC32 反向碰撞科普卡（analysis 类）。
 *
 * 覆盖 op：crc32Reverse
 * 纯数据无副作用，export default 对象照 eduContent.js 头注释契约。
 */
export default {
  crc32Reverse: {
    what: "CRC32 反向碰撞——给定目标 CRC32 校验值，反向推导出一段数据使它的 CRC32 恰好等于目标。与穷举式碰撞（crc32Collision）不同，本 op 用「反查表」直接求解，O(1) 查表不用逐个试。CTF 里常用于 ZIP 伪加密破解、文件头修复、伪造指定校验和的数据。",
    principle:
      "核心是标准 CRC32（poly 0xEDB88320，反射式/lsbit-first）的逆运算。\n\n" +
      "① 前向表：256 项标准 CRC 表 table[i]（i 从 0..255，每项按反射多项式迭代 8 位）。\n" +
      "② 反查表：对每个高 8 位值 m（0..255），收集所有使 table[j]>>24 == m 的字节 j —— 这是「逆推一步」的候选集。\n" +
      "③ 反向求解：从目标最终 CRC 值出发，沿反查表做 4 层 DFS（每层选一个字节 j，寄存器 = ((reg ^ table[j]) << 8)），到第 4 层反推 4 个原始字节，再用前向 calc 验证。\n" +
      "④ 由于 CRC32 有 2^32 个可能值而 4 字节补丁也是 2^32 种组合，**任意目标 CRC 都恰有（平均）1 个 4 字节解**——这是它能稳定求解的数学保证。\n\n" +
      "可选「可打印前缀」模式：在指定字符集内枚举 2 字符前缀，先算前缀 CRC，再反推 4 字节补丁（要求补丁字符也在字符集内），得到 6 字节可读碰撞串（如 ZIP 文件名伪造）。",
    usage: "输入目标 CRC32（hex，1-8 位，可带 0x 前缀）。点运行输出：① 4 字节补丁（hex 形式）；② 可打印前缀 + 补丁的可读碰撞串。选择前缀字符集参数可控制可读性（可打印 ASCII / 字母数字 / 纯数字 / hex）。",
    examples: [
      { in: "8e234ae0", param: "prefix=printable", out: "0e f5 08 69  →  \"!WE6AQ\" 等", desc: "标准测试目标，反推补丁验证 OK" },
      { in: "cbf43926", param: "prefix=printable", out: "2a 0d cd f2", desc: "CRC32(\"123456789\") 官方测试向量，反推验证 OK" },
    ],
    tips: [
      "任何 8 位 hex 都有 4 字节解（平均 1 个），不会无解——若输出「无解」先检查输入格式（1-8 位 hex，可带 0x）。",
      "ZIP 伪加密：把 ZIP 文件内某个文件的 CRC32 改成已知值，再反推伪造文件名——本 op 的「可打印前缀」模式正是为此设计。",
      "与 crc32Collision 的区别：那个是穷举式（逐长度 DFS），本 op 是表驱动解析解，快几个数量级；但本 op 固定输出 4 字节补丁（或 6 字节带前缀），长度不可任意。",
      "补丁是「任意数据」不是「可打印字符串」——需要可读串时用前缀搜索模式，且限制字符集越小越难有解（纯数字常无解）。",
      "CRC32 是线性校验和，无任何密码学强度：可被轻易伪造，别拿它当完整性校验。",
    ],
    aka: ["crc32Reverse", "CRC32 反向", "CRC 反推", "crc reverse", "CRC32 碰撞", "ZIP 伪加密", "crc32 伪造", "CRC 补丁", "crc32 collision", "反向 CRC", "CRC32 逆向", "crc brute", "校验和伪造", "crc32 构造", "4字节补丁"],
  },
};
