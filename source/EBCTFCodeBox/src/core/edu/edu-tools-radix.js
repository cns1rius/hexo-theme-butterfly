// 科普内容分片：工具类三件（radixAll / progCalc / unitConv，T337-T339 归并批）。纯数据，无 import 无副作用。
// 示例里的 in/out 全部是 node 直跑真实输出摘录，未编造。
export default {
  radixAll: {
    what: "一个输入丢进去，同时给你 2/8/10/16/32/36/62 进制的写法，外加 Base64、字节视图、码位、位宽 —— 不用再挨个换算工具来回切。",
    principle:
      "先做「进制嗅探」：`0x` 前缀当十六进制、`0b` 当二进制、`0o` 当八进制；空格/下划线/逗号这类分组符先删掉（`1_000_000` → `1000000`）。\n\n" +
      "**嗅探不出唯一解时不猜死一个**：像 `1010` 既可能是十进制的一千零一十，也可能是二进制的十，工具会把两种解读**并列列出**，各给一整份对照表。\n\n" +
      "全程用 `BigInt` 算，所以 $2^{256}$ 这种大数也不掉精度（位宽会老实报 257 bit）。负数走另一条路：给出 8/16/32/64 位的补码表示，因为负数的字节视图本来就依赖字宽。\n\n" +
      "⚠ 两个容易混的地方工具都标了：32 进制那行是**数值的 32 进制数字表示**，不是 RFC 4648 的 Base32 编码；Base64 那行是把数值按**最小大端字节**编码，不是「64 进制数字」。",
    usage: "直接输入一个数（支持 `0x`/`0b`/`0o` 前缀、十进制、带分组符）。裸的字母串（如 `ff`）会被拒绝并提示加前缀 —— 这是故意的，避免替你猜错进制。上限 4096 字符。",
    examples: [
      {
        in: "255",
        out: "2 进制 11111111 / 8 进制 377 / 16 进制 ff / 32 进制 7v / 36 进制 73 / 62 进制 47；Base64 /w==；数值字节 FF；UTF-16 00FF；U+00FF = \"ÿ\"；该码位的 UTF-8 编码 C3 BF；位宽 8 bit",
        desc: "注意「数值字节 FF」与「该码位 UTF-8 C3 BF」是两回事，别混",
      },
      { in: "1010", out: "[解读 1/2] 按十进制 → 16 进制 3f2；[解读 2/2] 按二进制 → 16 进制 a", desc: "歧义输入并列两解，不替你猜" },
      { in: "-1", out: "8 位补码 0xFF / 16 位补码 0xFFFF / 32 位补码 0xFFFFFFFF / 64 位补码 0xFFFFFFFFFFFFFFFF", desc: "负数走补码分支" },
    ],
    tips: [
      "CTF 里拿到一串不知道啥进制的数字，先丢这里看全解读，比一个个试快。",
      "位宽那行常用来判断「这个数塞不塞得进 32 位」—— 超了往往就是题目在暗示 64 位。",
      "要的是「文本 ↔ 逐字节进制」而不是单个数值？那是 `字符↔进制ASCII`（asciiRadix）；要手动指定源/目标进制两两互转用 `进制互转`（radixConvert）。",
    ],
    aka: ["一键进制转换", "进制转换器", "多进制对照", "进制对照表", "数字进制转换", "十进制转二进制", "十六进制转换器", "进制转换表", "基数转换", "多进制查看", "number base converter", "radix converter", "base conversion", "hex converter", "decimal to binary"],
  },

  progCalc: {
    what: "写代码/做逆向时那种「程序员计算器」：输入一个位运算表达式，直接给结果的十进制/十六进制/八进制/二进制以及补码、置位数等一堆视图。",
    principle:
      "支持 `& | ^ ~ << >> >>> + - * / % **`、括号，外加标准运算符没有、但 CTF 和密码学天天用的**循环移位** `rotl(x, n)` / `rotr(x, n)`。\n\n" +
      "**字宽可切 8/16/32/64 位**，所有中间结果都按字宽掩码截断回绕；**有/无符号**开关影响十进制显示与 `/ %` 的语义，`>>` 恒为算术右移、`>>>` 恒为逻辑右移。\n\n" +
      "两个工程上的硬要求：① 全程 `BigInt` + 掩码，**不用 JS 原生位运算** —— 因为 JS 的 `|`/`&` 会把操作数截成 32 位有符号，64 位字宽必错；② **不用 `eval`/`new Function`**，表达式走手写的词法分析 + 递归下降解析器（纯前端零外发的项目往里塞 eval 等于自己开洞）。",
    usage: "输入表达式，选字宽（默认 32 位）与有/无符号（默认有符号）。表达式上限 1024 字符、括号深度上限 32；非法输入给明确报错（哪儿错、为什么），不会白屏。",
    examples: [
      { in: "1 << 31", param: "32 位 / 有符号", out: "-2147483648（signed） / 2147483648（unsigned） / 0x80000000；二进制 1000 0000 0000 0000 0000 0000 0000 0000；置位数 1；尾随零 31" },
      { in: "rotl(0x80000000, 1)", param: "32 位", out: "1", desc: "循环移位：最高位转回最低位（普通 << 会直接丢掉）" },
      { in: "-8 >> 1", param: "32 位 / 有符号", out: "-4（算术右移补符号位）", desc: "同一输入换 >>> 得 2147483644，这就是两个右移的差别" },
    ],
    tips: [
      "算 CRC、哈希、分组密码时最常用 `rotl`/`rotr` —— 标准运算符没有它，别用 `<<` 加 `>>` 手工拼，容易错在符号位上。",
      "64 位场景一定要把字宽切到 64：很多在线计算器背地里用 32 位有符号，结果对不上就是这个原因。",
      "`置位数（popcount）` 那行在做汉明重量/汉明距离分析时直接能用。",
    ],
    aka: ["程序员计算器", "位运算计算器", "位运算求值", "位运算表达式", "进制计算器", "二进制计算器", "十六进制计算器", "整数计算器", "位操作计算", "循环移位计算", "popcount 计算", "bitwise calculator", "programmer calculator", "bit calculator", "hex calculator"],
  },

  unitConv: {
    what: "CTF / 逆向里真会用到的那几类单位换算：数据量、速率、时间、时间戳纪元、频率、角度。刻意没做成「通用单位换算大全」。",
    principle:
      "**数据量是重点**：1000 制（SI 词头，BIPM《国际单位制》第 9 版）与 1024 制（IEC 60027-2:2005 / IEC 80000-13:2008）**两制并排给**，因为这俩最常被搞混 —— 硬盘厂商标的 1 TB 是 $10^{12}$ 字节，系统显示的 931 GiB 是 $2^{30}$ 为底，数字对不上不是谁在骗人，是两套单位。\n\n" +
      "字节数全程 `BigInt` 有理数运算，PB 级不丢精度；除不尽时会明确标注是精确小数还是截断显示。\n\n" +
      "**纯数字输入会触发「时间戳纪元对照」**：按数量级嗅探是哪种纪元，然后把 Unix 秒 / Unix 毫秒 / Windows FILETIME（1601-01-01 UTC 起 100ns）/ Mac Cocoa（2001-01-01 起）/ Chrome μs（1601 起微秒）/ DOS 打包时间（FAT 目录项 16+16 位，2 秒精度）全列出来。\n\n" +
      "速率组注意 bit 与 Byte 差 8 倍（`Mbps` 不等于 `MB/s`）；角度组给度/弧度/百分度。每个系数在源码注释里都写了出处，不写「参考网上」。",
    usage: "输入「数值 + 单位」（如 `1 GB`、`100 Mbps`、`90 deg`）；输入**纯数字**则走时间戳纪元对照，可用参数强制指定纪元（默认 auto 按数量级嗅探）。",
    examples: [
      { in: "1 GB", out: "B = 1000000000；MB = 1000；MiB = 953.67431640625（精确）；KiB = 976562.5", desc: "同一份数据量，两制并排" },
      { in: "1 GiB", out: "B = 1073741824；MiB = 1024；KB = 1073741.824", desc: "1024 制这边才是整齐的 1024" },
      { in: "133000000000000000", out: "Unix 秒 1655526400；Unix 毫秒 1655526400000；Chrome μs 13300000000000000；Cocoa 秒 677219200；DOS 0x54D22354；ISO8601 2022-06-18T04:26:40.000Z", desc: "按 FILETIME 解读（1601 纪元，100ns）" },
    ],
    tips: [
      "取证题里看到 17~18 位的整数，多半是 FILETIME 或 Chrome 微秒（都是 1601 纪元）；10 位是 Unix 秒，13 位是 Unix 毫秒。",
      "算带宽别忘了 ÷8：100 Mbps 的线路满速也就 12.5 MB/s。",
      "ISO8601 那行只是辅助展示，要做 ISO ↔ 时间戳的双向转换用 timecodec 系的 op。",
    ],
    aka: ["单位换算", "单位转换", "数据量换算", "文件大小换算", "GB转GiB", "流量单位换算", "带宽换算", "网速换算", "时间单位换算", "时间戳转换", "纪元对照", "FILETIME转换", "角度换算", "弧度转换", "unit converter"],
  },
};
